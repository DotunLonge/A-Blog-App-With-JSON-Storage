// let listen = (element, what, arg) => {
//     element == 'doc' ? element = document : element = document.querySelector(element)

//     element.addEventListener(what, (event) => {
//         console.log(arg, event.target);
//     });
// }

// listen('button', 'click', `i've been clicked`);